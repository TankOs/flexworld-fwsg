#pragma once

namespace sg {

/** Triangle.
 * Only indices are stored.
 */
class Triangle {
	public:
		/** Ctor.
		 * @param a_ Vertex 0 index (a).
		 * @param b_ Vertex 1 index (b).
		 * @param c_ Vertex 2 index (c).
};

}
